name: Collector
on:
  workflow_dispatch:
  schedule:
    - cron: "*/15 * * * *"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    # 👇 دسترسی‌های لازم برای attest
    permissions:
      id-token: write
      attestations: write
      artifact-metadata: write
      contents: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '>=1.21.3'

    - name: Display Go version
      run: go version

    - name: Download dependencies
      run: go mod download

    - name: Tidy dependencies (regenerate go.sum)
      run: |
        rm -f go.sum
        go mod tidy

    - name: Build Go app
      run: go build -o myapp

    - name: Run collector
      run: ./myapp -sort -clash

    # 👇 مرحله attest در اینجا اضافه می‌شود (بعد از build و قبل از commit)
    - name: Attest Build Provenance
      uses: actions/attest-build-provenance@v4.1.0
      with:
        subject-path: 'myapp'

    - name: Commit changes
      run: |
        git config --local user.email "actions@github.com"
        git config --local user.name "GitHub Actions"
        git add telegram/ subscription/ mixed/ daily_archive/ config_cache.json clash-config.yaml stats.json go.sum
        git diff --staged --quiet || git commit -m "✔️ $(date '+%Y-%m-%d %H:%M:%S') Collected"

    - name: Push changes
      uses: ad-m/github-push-action@v1.1.0
      with:
        branch: main
        github_token: ${{ secrets.GITHUB_TOKEN }}
        force: false
